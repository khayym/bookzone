import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  root: {
    flex: 1,
    padding: 15,
    alignItems: 'center',
  },
  text: {
    fontSize: 16,
    marginTop: 20,
  },
  buttons: {
    width: '100%',
    borderWidth: 1,
  },
});
